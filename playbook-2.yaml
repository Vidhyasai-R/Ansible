- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      status: present
  - name: enabling nginx
    ansible.builtin.service:
      name: nginx
      status: started
      enabled: yes